{
    "name": "forex-bot-web",
    "version": "1.0.0",
    "description": "",
    "main": "build/app.js",
    "scripts": {
        "setup": "npm install && npm install browser-sync nodemon concurrently",
        "dev": "node scripts/build.js && chrome http://localhost:3000",
        "browser-sync": "node scripts/browser-sync",
        "dev-build": "node scripts/build",
        "buildHtml": "node scripts/buildHtml",
        "build": "node scripts/build && node scripts/buildHtml",
        "start": "npm run serve"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "moment": "^2.30.1",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "@types/node": "^20.11.27",
        "autoprefixer": "^10.4.18",
        "browser-sync": "^3.0.2",
        "concurrently": "^8.2.2",
        "esbuild": "^0.20.2",
        "node-sass": "^9.0.0",
        "nodemon": "^3.1.0",
        "tsconfig-paths": "^4.2.0"
    }
}